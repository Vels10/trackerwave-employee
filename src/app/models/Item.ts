import { Timestamp } from 'rxjs';

export interface Item{
  id ?: string;
  Name ?: string;
  Designation ?: string;
  Address ?: string;
  DOB ?: Date;
  Joining_date ?: Date;
  Employee_Id ?: number;
}
